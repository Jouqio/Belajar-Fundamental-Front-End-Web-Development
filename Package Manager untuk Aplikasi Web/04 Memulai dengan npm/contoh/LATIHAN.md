# Folder Latihan

Folder ini sengaja dikosongkan.

Ikuti instruksi di `README.md` modul ini:
1. Buka terminal, arahkan (`cd`) ke folder `contoh` ini.
2. Jalankan `npm init -y`.
3. Perhatikan file `package.json` baru yang otomatis muncul di folder ini.
